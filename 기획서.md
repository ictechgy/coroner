# coroner (가칭) — 프로덕션 크래시·행을 에이전트 회의실로 가져오는 부검 도구

- **작성일**: 2026-09-05
- **목적**: iOS 프로덕션 텔레메트리(`.ips` 크래시, MetricKit `MXDiagnosticPayload` — crash/hang/CPU exception/disk-write)를 로컬에서 수집 → 심볼리케이션 → 클러스터링 → 버전 비교하고, 코딩 에이전트가 MCP로 질의하게 만드는 오픈소스 기획
- **선점 여부 검증**: 2026-09-05 웹 검색. 부품은 전부 존재 — 심볼리케이션([MXSymbolicate](https://github.com/OliveTreeBible/MXSymbolicate), [MacSymbolicator](https://christiantietze.de/posts/2021/03/symbolicate-crash-logs/), `xcrun crashlog`), SaaS 수집(Sentry MetricKit 통합), 학술 dedup([GPTrace](https://arxiv.org/html/2512.01609v1)), 범용 트리아지([CASR](https://github.com/ispras/casr)), 그리고 **"포스트모템 크래시 → LLM/MCP" 패턴 자체는 [AMD Radeon GPU Detective](https://gpuopen.com/learn/post-mortem-gpu-crash-debugging-with-llms/)가 GPU 도메인에서 실증**. 그러나 "iOS 텔레메트리 → 심볼리케이션 → 클러스터 → 버전 저널 → 에이전트 digest" **통합 계층은 미점유** 확인. 빈 땅 등급 **⭐⭐☆** — 하향 사유: 크래시→LLM MCP 패턴의 실증 선례 존재, 상향 사유: iOS/MetricKit 도메인 통합은 확인상 공백이고 애플(ACP로 build/run만 흡수)·SaaS(수집이 본업) 모두 이 지점에 오지 않는 구조

---

## 한 줄 정의

> Sentry는 크래시를 대시보드에 가둔다. coroner는 부검 보고서를 에이전트 터미널로 가져온다 — "2.1.0의 신규 크래시 1건. SessionStore.dequeue(sessionStore.swift:88)의 인덱스 오버런. 2.0.9에서는 0건이었고, 이 빌드에 들어간 커밋은 이것."

---

## 문제 정의: 프로덕션 증거의 개발 컨텍스트 단절

1. **.ips/MetricKit은 Xcode Organizer에 갇힘** — 기기별로 흩어지고, TestFlight 유저 것은 전달이 늦으며(Sentry-cocoa 이슈에서도 확인되는 전달 타이밍 quirk), 질의 불가능한 덩어리
2. **심볼리케이션은 부품 단위로만 존재** — 스크립트·GUI 도구를 사람이 수동으로 잇는 수공업 파이프라인
3. **트리아지 지식은 사람 머리에만** — "이 크래시 알려진 건가? 이번 버전에 새로 생긴 건가?"의 답이 어디에도 기록되지 않음
4. **에이전트 시대의 결정적 단절** — 수정 작업은 코딩 에이전트에게 위임되는데, 에이전트는 Crashlytics/Sentry 웹 UI에 접근할 수 없다(인증+DOM). 즉 **증거를 볼 수 없는 주체에게 수정을 시키는 중**

yield-audit 문법으로: 이것은 verification gap 렌즈의 프로덕션 변주 — 검증의 입력(실패 증거)이 개발 컨텍스트에 도달하지 못해 발생하는 세금.

---

## 데이터 모델: 부검 보고서 1건 = YAML 1개

```yaml
id: c-2026-0905-f7a2
kind: crash                  # crash | hang | cpu-exception | disk-write
signature: "Fatal error: Index out of range @ SessionStore.dequeue"
top_frames:                  # 정규화 프레임 — 클러스터링 키
  - SessionStore.dequeue(sessionStore.swift:88)
  - SessionManager.flush(sessionManager.swift:41)
symbolicated: true
first_seen_build: 2.1.0 (142)
last_seen_build: 2.1.0 (143)
occurrences: {2.1.0: 41, 2.0.9: 0}     # "신규 유입" 판정의 근거
devices: {iPhone15,3: 22, iPhone14,2: 11}
os_versions: {iOS 19.1: 30, iOS 19.0: 11}
status: open                 # open | known | fixed-in
suspect_commit: 9f2e1a       # first_seen_build 전후 diff 교차 (v0.3)
digest: digest/2026-09-05.md
```

설계 결정:

- **클러스터 1개당 보고서 1개** — 부검 보고서는 증거의 요약이지 원본 덤프가 아님. 원본은 `inbox/`에 보존
- **occurrences를 빌드 키로 기록** — "new vs known"이 휴리스틱이 아니라 데이터에서 나옴
- **suspect_commit은 추정임을 명시** — 텔레메트리는 증거이지 판결이 아니다. 원인 단정 도구가 아닌 "수정 에이전트의 첫 페이지"

---

## 워크플로: 수집 → 부검 → 저널 → 조회

| 단계 | LLM | 내용 |
|---|---|---|
| 수집 | 불필요 | Organizer 내보내기 디렉토리 감시 / 기기 로그 직수집(xcrun devicectl) / MetricKit 전달자가 쓰는 페이로드 디렉토리 감시 |
| 부검 | 불필요 | .ips·MXDiagnosticPayload 파싱 → dSYM 자동 탐색(Spotlight + App Store Connect API, v0.3) 심볼리케이션 → 정규화 프레임 시그니처로 클러스터링(ReBucket류 스택 유사도) |
| 저널 | 불필요 | first_seen/last_seen 빌드 대조, 신규 유입 플래그. v0.3에서 전후 git diff와 교차해 suspect_commit |
| digest | 옵션 | 기간 요약 문서 생성. LLM 없어도 표·카운트만으로 성립 |
| 조회 | 불필요 | MCP 툴: `new_since(build)`, `top_crashes(n, scope)`, `crash_detail(id)`, `is_known(signature)`, `hang_report(period)`, `digest(period)` |

---

## 제품 원칙 (context-guard·yield-audit·tombstone의 문법 계승)

- **완전 로컬 기본**: 텔레메트리는 개발 기기의 부검실에만 존재. 원격 전송 0. Sentry/Crashlytics는 *수집* SaaS, coroner는 그 반대편(소비) 도구 — 경쟁이 아니라 파이프라인의 다음 칸
- **결정적 부분에 LLM 불요**: 파싱·심볼리케이션·클러스터링·저널은 전부 결정적. LLM은 digest 문장 다듬기에만 옵션
- **증거와 판결의 분리**: `suspect_commit`은 추정, 수정 제안은 하지 않음. 원인 규명은 에이전트 몫
- **프라이버시 기본 마스킹**: 기기 식별자·스택 내 사용자 문자열 기본 마스크, `.coronerignore`로 경로 제외

---

## 아키텍처 (v0.2 기준)

```
coroner ingest (watch)                    coroner query (MCP / CLI)
┌──────────────────────────────┐          ┌──────────────────────────────┐
│ sources  Organizer·devicectl  │          │ new_since(build)              │
│          ·MetricKit 페이로드   │          │ top_crashes / crash_detail    │
│ parse    .ips + MX payload    │          │ is_known(signature)           │
│ sym      dSYM 자동 탐색·적용   │ ───────▶ │ hang_report / digest          │
│ cluster  정규화 프레임 시그니처 │ reports/ │ suspect 교차 (git, v0.3)      │
│ journal  빌드별 first/last    │ *.yaml   └──────────────────────────────┘
└──────────────────────────────┘          저장: 완전 로컬. 언어: Swift
                                          (dSYM/indexstore-db 생태계와 동일 런타임)
```

---

## 로드맵

```
v0.1  .ips 파서 + dSYM 자동 탐색 심볼리케이션 + 시그니처 클러스터링
      + CLI(list/show/new) — "결정적 최소 부검실". crash만
v0.2  MetricKit 페이로드 수집(hang/CPU exception/disk-write) + Claude Code MCP 6툴
v0.3  버전 저널 고도화 — first_seen_build 전후 git diff 교차(suspect_commit)
      ← 심볼/인덱스 자산이 직접 먹히는 차별화 지점(indexstore-db로 프레임→소스 앵커)
      App Store Connect API dSYM 자동 다운로드
v1.x  CI 게이트(빌드마다 new_since로 신규 크래시 차단), macOS 앱 지원,
      클러스터링 옵션(임베딩 유사도 — GPTrace 계승 명시), SaaS 역링크 내보내기
```

---

## 차별화·경계 선언 (README에 명시할 것)

| 인접 | 그들과의 차이 |
|---|---|
| **Sentry/Crashlytics MetricKit 통합** | *수집·대시보드* SaaS. 에이전트가 접근 불가하고 로컬이 아님. 우리는 소비 측 로컬 도구 — SaaS 사용자에게도 "그 데이터를 에이전트에게 주는" 가치가 독립 성립 |
| **Xcode Organizer** | 수동·질의 불가·기기 묶음 뷰어. 우리는 질의 가능한 원장 + 에이전트 인터페이스 |
| **MXSymbolicate / MacSymbolicator / xcrun crashlog** | 파일 1개 부검 부품. 클러스터링·버전 저널·MCP 없음 — coroner가 *사용하는* 부품이지 경쟁이 아님 |
| **CASR** | 범용 크래시 트리아지. iOS .ips/MetricKit 포맷 없음, 에이전트 인터페이스 없음 |
| **Radeon GPU Detective MCP** | 같은 패턴의 GPU 도메인 실증(정직하게 인용). 우리는 앱 텔레메트리 도메인 + 버전 저널 + suspect 교차 |
| **GPTrace (학술)** | LLM 임베딩 dedup 논문 — v1.x 클러스터링 옵션의 인용 대상 |
| **tombstone (포트폴리오)** | 프로덕션 증거(coroner) ↔ 기각 원장(tombstone). coroner가 찾은 버그의 수정 시도가 기각되면 tombstone에 묘비 — 상호 판매 구조 |

---

## 리스크와 대응

- **포맷 취약성**(.ips/MetricKit 스키마가 OS 버전마다 변함 — Sentry-cocoa #1661/#8468급 quirk 다수) → 실제 페이로드 코퍼스 스냅샷 테스트 + 버전별 파서 어댑터
- **dSYM 확보 마찰**(보관 안 하면 심볼리케이션 불가) → v0.3 App Store Connect API 자동 다운로드. bitcode 폐지 이후 관리 부담은 축소 추세
- **유입 병목**(MetricKit은 일간 미만 전달, TestFlight 지연) → 기대치 관리: 실시간이 아니라 **일간 부검실**. Organizer 내보내기·기기 직수집 경로 병행
- **선점 가속**(Sentry가 로컬 MCP 추가 가능성) → 차별선을 suspect_commit 교차(정적분산)와 에이전트 워크플로 원장에. SaaS는 로컬-퍼스트를 안 한다
- **오트리아지**(클러스터 과소/과대 병합) → 시그니처는 결정적으로, 임계값 조정은 CLI로 노출, 원본 항시 보존

---

## 이름 후보

`coroner`는 tombstone과 같은 어휘 계열(죽은 프로세스의 부검)이어서 포트폴리오 서사가 맞물림. 후보:

1. **`coroner`** (부검의사 — 포스트모템 분석의 본질과 정합. PyPI/Crates/GitHub 오염 확인 필요)
2. `postmortem` — 범용어라 검색 오염 큼
3. `blackbox` — Blackbox AI와 충돌, 배제

저장소 개설 시 GitHub/PyPI/Homebrew 가용성 확인 후 확정.

---

## 첫 공개 전략

자기 TestFlight 앱의 **실제 부검 보고서**를 README에 싣는다. 후킹 한 줄("수정 에이전트에게 검안서를 보여줘라") + MCP 설정 3줄. 데모 GIF: 새 빌드 업로드 → `coroner new_since`가 신규 크래시 1건과 suspect 커밋을 지목 → 에이전트가 해당 심볼을 열어 수정. yield-audit verification gap 렌즈 리포트를 전 증거로 함께 인용.

**포트폴리오 내 위치**: context-guard(입력 위생) → yield-audit(운명 측정) → **coroner(프로덕션 증거의 유입)** → tombstone(실패 지식의 영속화). "에이전트×모바일" 축의 두 번째 기둥.
